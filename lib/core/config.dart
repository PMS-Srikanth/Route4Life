class AppConfig {
  static const Duration requestTimeout = Duration(seconds: 10);
  static const Duration locationUpdateInterval = Duration(seconds: 5);
  static const int maxHospitalRequests = 5;
  static const int rankingTopN = 3;
}
